extends layout

block content
    h1= title
    p Welcome to #{title}

    form#app_form(action="#")
        label New Task:
            input#add_task(type="text")
        input(type="submit")
        ul#tasks_block